# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# `dist` is the published package output — gitignored because it's a
# build artefact, but npm publish still ships it via the "files" field
# in package.json.
dist
*.local

# tsc -b incremental build cache
*.tsbuildinfo

# Env files
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
